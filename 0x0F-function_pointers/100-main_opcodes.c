#include "function_pointer.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - E
 *
 * @argc: ..
 * @argv: ..
 * Return: 0.
 */

int main(int argc, char **argv)
{
	char *s = (char *)main;
	int i;

	if (argc != 2)
		printf("Error\n"), exit(1);
	i = atoi(argv[1];

			if (i < 0)
			printf("Error\n"), exit(2);
	while (i--)
	printf("%02hhx%s", *s++, b ? " " : "\n");
	return (0);
}
